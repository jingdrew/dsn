type User {
    username: String
    password: String
}

input UserInput {
    username: String!,
    password: String!,
    firstName: String!,
    lastName: String!,
    email: String!,
}

type Query {
    user: User
}

type Mutation {
    signUp(input: UserInput!): User
}